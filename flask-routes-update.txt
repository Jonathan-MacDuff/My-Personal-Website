# Add these routes to server/app.py after the Pet-Finder routes:

# Cat-Meme-Generator SPA routes
@app.route('/Cat-Meme-Generator/static/<path:filename>')
def serve_catmemes_static(filename):
    print(f"🟦 Serving Cat-Memes static asset: /Cat-Meme-Generator/static/{filename}")
    return send_from_directory('static/cat-meme-generator/static', filename)

@app.route('/Cat-Meme-Generator', strict_slashes=False)
@app.route('/Cat-Meme-Generator/', strict_slashes=False)
def serve_catmemes_root():
    print("🟩 Serving Cat-Memes root")
    return send_from_directory('static/cat-meme-generator', 'index.html')

@app.route('/Cat-Meme-Generator/<path:path>', strict_slashes=False)
def serve_catmemes_spa(path):
    print(f"🟨 Cat-Memes SPA route: /Cat-Meme-Generator/{path}")
    # Check if it's a static asset file (favicon, manifest, etc.)
    if '.' in path and '/' not in path:
        print(f"🟧 Trying to serve Cat-Memes asset: {path}")
        try:
            return send_from_directory('static/cat-meme-generator', path)
        except:
            print(f"🟥 Cat-Memes asset not found: {path}")
            pass
    # For all SPA routes, serve index.html
    print(f"🟪 Serving index.html for Cat-Memes SPA route: {path}")
    return send_from_directory('static/cat-meme-generator', 'index.html')